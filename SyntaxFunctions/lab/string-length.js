function totalLength(a, b, c) {
    let countLetters = a.length + b.length + c.length;
    let average = Math.floor(countLetters / 3);
    
    logT
    console.log(countLetters);
    console.log(average);
}

totalLength('chocolate', 'ice cream', 'cake')